# S1-01: Set up Cargo workspace and crate structure

**Story ID:** S1-01 (Sprint 1, Story 1)
**Status:** Done
**Epic:** MVP 1 - Reactive Core
**Points:** 3

## Description

Create the foundational Cargo workspace with the four main crates:
- `domius-core` — Signals, Effects, and runtime
- `domius-macro` — Procedural macro for `domus!`
- `domius-web` — Component system and DOM bindings
- `domius-cli` — Code generation tool

## Acceptance Criteria

- [x] Root `Cargo.toml` configured as workspace with 4 members
- [x] `domius-core/Cargo.toml` created with dependencies:
  - `wasm-bindgen`
  - `web-sys` (with DOM features)
  - `js-sys`
- [x] `domius-macro/Cargo.toml` created with dependencies:
  - `proc-macro2`
  - `syn`
  - `quote`
- [x] `domius-web/Cargo.toml` created with `domius-core` as dependency
- [x] `domius-cli/Cargo.toml` created as binary
- [x] All crates compile without warnings
- [x] Each crate has `src/lib.rs` with module declarations

## Implementation Notes

- Use `cargo-generate` or manual setup
- Configure for WASM target: `wasm32-unknown-unknown`
- Add `.cargo/config.toml` with opt-level = 3 for release builds
- Create `examples/` directory structure

## Dev Plan

- Task: Implement core workspace changes and verify compilation (completed)
- Task: Add minimal tests for `signal` module
- Task: Commit incremental changes per subtask

## Implementation

Completed: Workspace and crate manifests exist; basic module files present.
Dev work will be tracked in commits; initial plan created.

## Dependencies

None — this is the foundation

## References

- Cargo book: https://doc.rust-lang.org/cargo/
- wasm-bindgen book: https://rustwasm.org/docs/wasm-bindgen/
