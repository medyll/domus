[package]
name = "domius-core"
version = "0.1.0"
edition = "2021"
description = "Platform-agnostic reactive runtime for Domus — signals, effects, scopes"
license = "MIT"

[dependencies]
# No external dependencies — 100% Rust std!
serde = { version = "1.0", features = ["derive"], optional = true }

[features]
serde = ["dep:serde"]

[dev-dependencies]
# Native tests only — no WASM runtime required
